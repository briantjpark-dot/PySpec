from spec_models import *

def count_open(tasks: list[Task]) -> int:
    """Count how many tasks are still open, meaning not yet done"""
    raise NotImplementedError

def highest_priority(tasks: list[Task]) -> str:
    """Return the title of the task with the highest priority number"""
    raise NotImplementedError
