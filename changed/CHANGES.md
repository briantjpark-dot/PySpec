# Changed functions to re-implement

These functions were added or changed in the spec. Re-implement
each one so its tests in `test_changed.py` pass. Leave every
other function in the project alone -- they did not change.

## Affected functions

- count_open
- highest_priority

## Functions removed from the spec

These no longer exist in the spec. You may delete them and their old tests from the main project:

- all_titles